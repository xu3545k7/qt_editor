# Nostalgia 製譜器 使用說明

> 適用版本：`NostalgiaChartEditor.exe`
> 內部音高一律用 MIDI（21\~108），畫面預設顯示鋼琴鍵號（1\~88）。

---

## 一、開始

### 匯入 MIDI

拖進來或從「檔案 → 開啟」選 `.mid`，會先問你：

| 選項 | 行為 |
| --- | --- |
| **自動轉譜（Eather 風格）** | 裁掉踏板長音 → 自動排譜。靠**收窄**擠空間、幾乎不重疊、表情記號自己標 |
| **自動轉譜（官方風格）** | 同上，但改用官方語料的做法：靠**鍵道重疊**擠空間、只有單手同時 4 音才收窄、自動標滑音 |
| **不轉換（MIDI 編輯模式）** | 只匯入音符，保留原始音高與時間，之後隨時可再排譜 |
| 取消 | 不載入 |

選「不轉換」會**鎖在音高模式**，因為其他檢視看到的鍵道位置只是暫時排開的。想切到小節／時間檢視時會再問一次要不要轉譜。

> 轉譜是一段不可細分的 CPU 工作，密集曲目約 **30\~60 秒**。會顯示進度視窗與已經過秒數，可取消。

### 樂器圖例（多軌 MIDI）

匯入多軌 MIDI 而**還沒排譜**時，右上角會出現一個小框，標出**哪個顏色是哪個樂器**。

* 名稱取自 MIDI 的 `track_name` / `instrument_name`；沒有名稱才退回 `program_change` 的 GM 音色名
* 兩者都有而且不一樣時會並列，例如 `Melody（Violin）`。但 program 0（Acoustic Grand Piano）是幾乎所有 MIDI 的預設值，不會接上去
* 軌數多的時候會自動分欄（實測 Designant 有 20 軌：Bass / Drum / Pad / Lead / KICK…）

> 只有「照聲部上色」時才畫。排過譜之後一律紅藍分左右手，圖例對不上就不出現；單軌 MIDI 分不出聲部、上色會退回照音高切一刀，那時候兩個顏色其實是同一個樂器，所以也不畫。

### 疊加匯入 MIDI（無主音）

「檔案 → 匯入 MIDI 音軌 → **疊加匯入 MIDI（無主音）**」。

和同一個選單裡的「開啟右手／左手 MIDI」不一樣：那兩個是**取代**該手的全部音符，這個是**純粹加上去** —— 現有的一顆都不會少。

新來的音符怎麼分手：

| 來源 MIDI | 結果 |
| --- | --- |
| **剛好兩軌** | 那兩軌就是左右手（和自動排譜同一條規則），照它的分配匯入 |
| 單軌、或三軌以上 | 分不出來，一律標成**無主音**（`hand=2`），畫面上是獨立的**綠色**，之後可以自己指定左右手 |

> `hand=2` 是官方資料本來就有的值（官方 normal 譜有 54% 的音符是這樣）。以前編輯器會把它畫成左手，疊上去之後分不出哪些是新來的，現在有自己的顏色。

鍵道不動：疊上來的音符沿用轉檔器給的位置，要排譜的話再按自動排譜。

> **有無主音就不能存檔或匯出。** 那是「還沒決定用哪隻手」的中間狀態，帶著它存檔等於把未完成的譜交出去。按存檔／匯出時會擋下來，**跳到最早的那一顆並選起來**，按 `L`／`R` 指定左右手即可。全部指定完就能存了。
>
> 注意：官方低難度譜本來就大量使用 `hand=2`（實測一份 normal 譜有 73 顆），所以直接打開官方 normal 譜編輯也會被擋。

### MIDI 解析的兩個清理

* **同音配對用先進先出**：圓滑奏（下一顆已經響了、前一顆的 note_off 才晚一兩個 tick 到）以前會被配錯，解析成「前一顆吃掉整段 + 新的那顆只有 1ms」的鬼音。現在兩顆都是正常長度。
* **同刻同音的極短分身會被丟掉**：同一 tick、同一音軌、同一音高被敲兩次，其中一顆只有幾毫秒（≤20ms 且另一顆至少 5 倍長）—— 那是 MIDI 產生器的殘渣，留著會讓玩家為同一個聲音按兩下。**兩顆都是正常長度的重複不會動**，那可能是刻意疊軌。

### 開啟既有譜面

支援 `.xml`（官方格式）與 `.json`（遊戲格式）。兩者音高都能正確讀取。

---

## 二、檢視模式

`Tab` 循環切換，或用工具列的「檢視」分頁。

| 模式 | 橫軸 | 用途 |
| --- | --- | --- |
| **小節** | 遊戲鍵道 0\~27 | 實際譜面，所見即所得 |
| **時間** | 遊戲鍵道 0\~27 | 同上，但時間等距 |
| **音高** | 鋼琴 88 鍵 | 看音樂本身，底部有鋼琴鍵盤 |

### 音高模式

* 底部固定 168px 的鋼琴鍵盤，**上緣釘在判定線**
* 播放時音符碰到判定線，對應的琴鍵會**亮起該聲部的顏色**
* 平常是全黑背景；**開啟放置模式**後，格線只在滑鼠附近顯示並跟著游標走

### 延音踏板欄（音高模式）

左側那條窄欄就是 CC64。

* **拖曳**＝加一段踏板；**點一下**已有的區間＝刪掉它
* 判定線正踩在某段上時，那段會亮成淺琥珀色
* 右鍵 → **延音踏板**：由選取音符建立 ／ 由整首音符長度推算 ／ 清除全部

播放時**放開琴鍵那一刻踏板還踩著的話，殘響延續到放開踏板為止**。判斷點是音符自己的收音時刻（含 gate／斷奏修正過的），不是起音。

匯入 MIDI 時 CC64 會自動抽成區間；匯出時寫回 track 0 的 CC64。**XML／JSON／MIDI 三種格式都存得住**，詳見〈六、力度與踏板的存檔〉。

可在「偏好設定 → 延音踏板」關掉這一欄。

### 力度

音高模式下，音符會**依 velocity 調整明暗**——弱音偏暗、強音偏亮。只有音高模式這樣做：小節／時間模式看的是「要按哪裡」，把力度混進顏色只會干擾左右手的紅藍判讀。

右鍵 → **設定力度**：

| | |
| --- | --- |
| ppp 16 … fff 127 | 一鍵設成常用的力度記號 |
| 加重 ＋10 ／ 減輕 −10 | 相對調整，保留起伏 |
| 自訂力度… | 指定 1\~127 |
| 依比例縮放… | 按百分比乘，**整段調亮／調暗時用這個** —— 設固定值會把演奏的表情整平掉 |

**滾輪調力度**：游標停在**已選取的音符**上滾動，直接改力度（一格 ±4，按住 **Shift** 變成 ±1）。多選會一起動，維持彼此的強弱關係。連續滾動只算一筆 undo。

> 條件是「游標壓著選取的音符」，不是「有東西被選取」——後者會讓選了音符之後整個畫面都捲不動。游標不在音符上時滾輪照舊捲動，Ctrl＋滾輪照舊縮放。

沒有 velocity 的音符（純遊戲譜 XML）一律當作全力度，不變色。

### 力度與踏板的存檔

| | 力度 | 踏板 |
| --- | --- | --- |
| **MIDI** | note_on velocity | track 0 的 CC64（多軌各寫一份會讓同段殘響被踩放好幾次，聽起來像斷奏） |
| **JSON** | `notes[].velocity` | `pedal_data` |
| **XML** | `<note><velocity __type="u8">` | `<pedal_data>`（`note_data` 的兄弟節點） |

兩者都是**沒有資料就完全不寫**——現有譜面存檔不會多出任何節點或欄位（實測官方 `birth_of_devil_03real` 存回去，根層節點與 note 欄位一字不差）。

幾個為什麼：

* **XML 的 `velocity` 借用官方欄位名**。官方 `<note>` 沒有力度，但官方 `sub_note` 有 —— 64.8 萬個 sub_note 全都帶 `<velocity __type="u8">`，值就是 MIDI 力度（最常見 100／110／90）。沿用同一個名字與型別，語意才一致。
* **`<pedal_data>` 是自訂節點**。官方根層本來就分成 header／note_data／event_data／beat_data／track_info／velocity_zone_data 六段，多一段是這個格式自己的形狀；遊戲端一律用名字取節點（`root.Element("note_data")`、`TryReadInt`），不認得的節點碰都不會碰到。欄位排法照抄官方的 `velocity_zone`。
* **官方的 `velocity_zone_data` 不是踏板**。名字很像所以我去量了：2479 份官方譜裡 2122 份有，每首中位 9 段、每段中位 3.0 秒、彼此不重疊、覆蓋全曲約 47%，`velocity_type` 只有 0 和 1（type0 的 gate 中位 216ms、type1 是 138ms）。真正的踏板應該是每次和聲變化就一段、一首幾十到上百段 —— 對不上，所以沒有拿它來裝踏板。

可在「偏好設定 → 力度表現」關掉上色。

### 音高編號

**檢視 → 音高編號**：

* **鋼琴 (1\~88)** — 預設，和遊戲、XML 的 `scale_piano` 一致
* **MIDI (21\~108)** — 對照原始 MIDI 時用

兩者固定差 20。只影響顯示，不影響存檔。

---

## 三、分割與視窗

工具列的「分割」按鈕循環四種版面：

1. **左右分割**
2. **上下分割**
3. **獨立視窗** — 兩個 top-level 視窗
4. **疊層分割** — 底層實色、上層半透明；按 **SHIFT** 對調哪一層透明

兩格的捲動與縮放連動，音高模式那格會框出小節模式的可見範圍。工具列的紅／藍色塊決定指令作用在哪一格。

---

## 四、編輯

### 放置音符

工具列「放置模式」開啟後，點擊即放置。

* **拖曳**可拉長時值；**tap 一旦被拉長會強制變成 hold**
* 預覽方塊和實際音符長得一樣（白色圓角框＋聲部配色）
* 沒有譜面時放置會先跳「新增譜面」

### 音符類型

`T` tap ／ `H` long ／ `K` staccato ／ `L` 左手 ／ `R` 右手，或右鍵 →「音符屬性 → 類型」。

### 遊戲譜面隱藏 NOTE

> 隱藏的是**按鍵**，不是**聲音**。玩家少按一下，但和絃完整發出來 —— 這正是官方低難度的做法。

右鍵 → 音符屬性 → **遊戲譜面隱藏 NOTE**（支援多選）。

| | 行為 |
| --- | --- |
| 音高模式 | 留在原本的音高位置、**半透明**，並拉一條**白色虛線**連到寄主 |
| 其他模式 | **完全不畫**，只看得到寄主 |
| 播放 | **照常發聲** |
| 存檔 | 併入音高最接近的可見音符的 `sub_note_data` |

寄主 = 同一時刻（±120ms）音高最接近的可見音符；隱藏音自帶多音時取**平均音高**比對。

**拆出**：選到寄主時，右鍵會多一個「拆出隱藏音符（N 個）…」。超過 3 個會跳複選清單讓你挑。

> 打開官方 normal 譜會看到大量隱藏音（例如 chopin_nocturn2 是「可見 365 + 隱藏 321」）—— 那些原本埋在 `sub_note` 裡看不到的音，現在都能檢視和調整。

---

## 五、播放

### 音源

右上角三條音量，每條都有喇叭開關，預設全開：

* **音樂音量** ／ **音源2 音量** — 沒載入音樂檔時整列灰掉
* **鋼琴音量** — MIDI 音源，**沒有 WAV 也能用**

### 沒有音樂檔也能播

按播放時若未載入 WAV，會問「載入音樂檔 ／ 只播 MIDI ／ 取消」，預設**只播 MIDI**。

### 播放中編輯

播放途中新放置的音符會**立刻用鋼琴音發聲**。

### 右鍵播放 MIDI

框選範圍後右鍵，只播該範圍內的音符，時值與力度照音符本身的資料。

---

## 六、匯出

「檔案 → 匯出曲目」會產生完整曲目資料夾，並自動在 `難度/source/` 底下**備份 XML 與 MIDI** 作為譜面來源。

> 強烈建議保留 `source/`。曾經有一份譜面因為沒有來源檔，音高損壞後無法還原。

---

## 七、自動排譜的規則

### 兩套風格

「設定 → 偏好設定 → 轉譜風格」可以選預設值，匯入 MIDI 時也會直接問（記住上次的選擇）。

| | Eather 風格（預設） | 官方風格 |
| --- | --- | --- |
| 擠空間的手段 | **收窄**（整組跨度 7\~11 半音就收成寬度 2） | **讓鍵道重疊**（只有剛好單手同時 4 音才收窄） |
| 寬度 2 的比率 | 約 23%（Eather 手寫譜語料 14.6%） | 約 4%（官方 real 語料 2.7%） |
| 同手和絃鍵道重疊 | 幾乎沒有（Eather 手寫譜 0.8%） | 允許（官方 k=5 有 72.6%、k=6 100%） |
| 滑音 | **不自動標** | **自動標**（官方 real 有 0.8%、hard 到 8%） |
| soft／staccato | 不自動標（官方資料裡根本沒有這兩種型別） | 同左 |
| 前後同音同軌 | 最高優先，最後再收一次 | 不特別強制 |

**共用的部分**：和絃內部幾何、左右手分配上限、度數階梯表、長押走廊、鍵道範圍。那些是官方語料量出來的物理事實，兩種風格都遵守。

### 共用規則

轉譜時套用的主要規則（皆以 423 份官方 real 譜、39.9 萬對相鄰音實測校準）：

| 項目 | 規則 |
| --- | --- |
| 音符寬度 | real 一律 3 鍵；**剛好單手同時 4 音**才收窄成 2（官方 88.4% 如此）。5 音以上不再收窄，改用鍵道重疊（官方 k=5 有 72.6% 重疊、k=6 100%）。**前後兩顆音的小度數不用收窄表現**（官方單手同時 1／2 音的收窄率是 0.0%／0.2%），一律改用位置 |
| 寬度還原 | 2 → 3 一律**往左（低音側）長**，右緣 `max_key` 不動 —— 那條線是排序與視覺的權威 |
| 左右手分配 | 同時發聲的音符盡量平均分給兩手，單手上限 `max(3, ⌈N/2⌉)`（官方 4 音 51% 排 2/2、5 音 91% 排 3/2，單手拿 4 顆只佔 0.63%） |
| 前後鍵道距 | 約 **半音數 ÷ 2**；超過八度後斜率降到 0.25 |
| 同手和絃內部 | **看顆數，不看音程**。兩顆照階梯表：≤4 半音 3 格、5\~7 半音 4 格、≥8 半音一律 5 格。**3 顆以上完全貼合** —— 官方 k=3 相鄰兩顆的中心距固定 3 格，**八度和半音一模一樣**（12 半音 n=924 也是 3.0），外圍中心距固定 6 格，中間那顆在正中央。度數只在「同手兩顆」和「跨手」表現 |
| 何時收窄 | 同手同時發聲、**整組跨度 7\~11 半音**（不到一個八度、但也不是擠在一起的短度數）時整組收窄成寬度 2。**度數短又有空間就不收窄** —— 那種用位置排得開。跨度 ≥12 半音維持寬度 3。真的塞不下時另外由容量預算強制收。設定：`narrow_span_min` / `narrow_span_max` / `narrow_whole_span_min` / `narrow_whole_span_max` |
| 前後同音同軌 | 同一個音高前後出現時要落在**同一個鍵道**，這是最高優先。`_snap_repeated_pitch_lanes` 排在所有搬動音符的通道之後再收一次，直接對齊前一次出現（不是視窗中位數）。搬法由輕到重：整隻手 → 整組（含另一隻手，跨手距離不變）→ 單顆。它自己會守住最高音排序、和絃貼合、度數間距與兩手交界，只准變好不准變壞。設定：`snap_repeat_final` |
| 單手最高音 | 同一隻手、同一時刻的最高音，前後之間**必須嚴格分得出高低**，連「同格」都不行（56 / 54 / 59 → 22 / 21 / 23）。最後一道 `_repair_hand_top_edge_strict` 專門收這個：優先整組剛體平移（不動和絃幾何），移不動才單獨移最高音那一顆 |
| 度數不准被擠掉 | 後段每一道順序／輪廓修復都會檢查「同手兩顆的度數間距」，只准維持或變好。官方 ≥8 半音的同手雙音有 85.7% 剛好 5 格、只有 3.7% 貼著 |
| 跨手 | 同度數比同手多 **2 格**，下限 5 格 |
| 兩手間距 | 中位 7 格，交叉率 < 0.4% |
| 同音高位置 | 同一隻手的最高音，同一個音高在 **2 秒內不換位置**（官方同音鍵道差中位數 = 0）。位置一致性是最後一道修復，排在輪廓與交界之後 |
| 單手跨度 | 極少超過一個八度（官方 90 分位剛好 12 半音） |
| 踏板長音 | 長音中間出現同音高、或單手構不到的音 → 裁到下一顆前 100ms |
| 表情記號 | **soft／staccato／滑音都不自動寫**，交給人判斷 —— 那是演奏詮釋，猜錯要一顆一顆改回來。判斷邏輯留著，開 `classify_soft` / `classify_staccato` / `classify_slide` 就會回來。**hold 仍然自動寫**（那是 MIDI 長度直接看得出來的事實），trill 本來就只統計不寫入 |

實測相對官方語料：**反向錯序在每個時間跨度都低於官方**，樂句最高／最低音正確率 85%（官方 78\~82%）。

---

## 八、疑難排解

**轉譜要 30\~60 秒**
正常。進度視窗會顯示已經過秒數，不是當掉。

**兩個格子的音符顏色不一樣**
排譜後一律以左右手上色（右紅左藍）。只有「未排譜的 MIDI 編輯模式」才按音軌分色 —— 那時候還沒有左右手可言。

**打開 MIDI 全是同一個顏色**
該檔案是 format 0（單軌單通道），沒有可用的聲部資訊。編輯器會退而用左右手、再退而用音高中位數分色。

**換語言**
「設定 → 偏好設定」切換後**立即生效，不需重開**。

**音高數字和預期差 20**
檢視 → 音高編號，切換鋼琴 (1\~88) 與 MIDI (21\~108)。

---

## 九、已知限制

* 轉譜時間較長（密集曲目 50 秒上下）
* `restore_pitches_from_midi()`（用原始 MIDI 比對還原音高）目前**只有 API、沒有選單入口**。它會自動偵測「譜面整份被平移」（實測曲庫有 5 份開頭補了空白，2750\~6850ms 不等）與「被拉伸但發音組數相同」兩種情形；實測 37 份沒有音高的譜可以還原 97.3%，26 份到 100%。真正還原不了的只有「非等量重定時」的譜
* 難度分級（normal／hard／extreme 的寬度與密度差異）尚未實作，目前只針對 real 調校
