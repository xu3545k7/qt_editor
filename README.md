# NOS Chart Maker（qt_editor）

給 Nostalgia 類型鋼琴音樂遊戲用的譜面編輯器。可以把 MIDI 轉成遊戲譜面、
用鋼琴捲軸的方式編輯、聽內建鋼琴音源試聽，再匯出成遊戲吃得下的 XML / JSON。

---

## 目錄

- [這是什麼](#這是什麼)
- [安裝與執行](#安裝與執行)
- [五分鐘上手](#五分鐘上手)
- [三種檢視模式](#三種檢視模式)
- [工具列分頁](#工具列分頁)
- [編輯操作](#編輯操作)
- [播放](#播放)
- [工具選單](#工具選單)
- [偏好設定](#偏好設定)
- [快捷鍵](#快捷鍵)
- [匯出](#匯出)
- [開發](#開發)

---

## 這是什麼

一份 MIDI 進來，一份可以直接放進遊戲的譜面出去。中間你可以：

- 把 88 鍵的鋼琴音高**排成遊戲的 28 條鍵道**（自動排譜，兩種風格可選）
- 用**音高模式**像鋼琴捲軸一樣看譜、改音高
- 編輯**力度**（velocity）與**延音踏板**，並用內建音源試聽
- 調整 **BPM、拍號、小節**，包括多小節批次修改
- 匯出成 **XML / JSON 譜面**、**MIDI**，或連同音訊打包成完整曲目

譜面格式支援官方 XML（含 `sub_note` 的一鍵多音、trill、slide）與本專案的 JSON。

---

## 安裝與執行

### 直接用打包好的執行檔

`dist/NostalgiaChartEditor.exe` 打開就能用，不需要裝 Python。
第一次啟動要解壓縮，可能要等 30～60 秒。

### 從原始碼跑

需要 **Python 3.11**（開發環境是 3.11.9）。

```bash
pip install PyQt5 mido
pip install simpleaudio          # 選用：沒有的話會退回 winsound
python -m qt_editor.app
```

也可以直接帶檔案路徑開啟：

```bash
python -m qt_editor.app path/to/chart.xml
```

| 套件 | 必要性 | 用途 |
|---|---|---|
| `PyQt5` | **必要** | 整個介面 |
| `mido` | **必要** | 讀寫 MIDI |
| `simpleaudio` | 建議 | 音訊播放後端；沒有的話用 winsound，功能較少 |
| FluidSynth | 選用 | 內建鋼琴音源（試聽、匯出鋼琴音軌）。DLL 放在 `vendor/fluidsynth/`，用 ctypes 載入 |

沒有 FluidSynth 一樣可以編譜，只是聽不到鋼琴音、也不能匯出鋼琴音軌。

---

## 五分鐘上手

1. **檔案 → 匯入 MIDI**，選一個鋼琴 MIDI。
   - 兩軌的 MIDI 會直接把兩軌當成左右手（這是最常見的情況，也最準）。
   - 軌數不是兩軌時，會照各軌的平均音高分手。
2. 程式會問**要不要自動排譜**。答應的話，音高會被排到 28 條鍵道上。
   - 不排譜也可以，但那樣只有**音高模式**看得到東西（其他模式的鍵道位置是假的）。
3. 按 <kbd>1</kbd> 切換檢視模式，找到你習慣的看法。
4. 按 <kbd>Space</kbd> 試聽。
5. 用 <kbd>2</kbd> 開放置模式加音符，或直接拖拉既有音符調整。
6. **檔案 → 另存新檔**存成 XML 或 JSON。

---

## 三種檢視模式

按 <kbd>1</kbd> 循環切換，或用「檢視」分頁的按鈕。

| 模式 | 縱軸 | 適合 |
|---|---|---|
| **小節** | 每小節等高 | 對齊小節、看譜面結構 |
| **時間均分** | 每毫秒等高 | 看實際節奏密度、對音訊 |
| **音高** | 每毫秒等高，橫軸是 88 鍵 | 改音高、力度、踏板 |

**音高模式**是鋼琴捲軸：橫軸直接對應鋼琴鍵，底部有一排真的琴鍵，音符落到判定線時會亮起來。
左側是**延音踏板欄**，左右兩側邊緣是**力度曲線欄**（左手在左、右手在右）。

判定線永遠固定在**鍵盤上緣**——音符碰到鍵盤頂端就是判定的那一刻。

---

## 工具列分頁

| 分頁 | 內容 |
|---|---|
| **編輯** | 調性顯示、放置模式、音階輔助、手別篩選、基本排序 |
| **播放** | 播放全曲 / 播放視窗 / 暫停 / 停止、打擊聲開關、播放偏移 |
| **小節** | 新增/刪除小節、設定單一小節 BPM、多小節 BPM/拍號、拍號設定 |
| **工具** | 見[工具選單](#工具選單) |
| **檢視** | 分割畫面、縮放、預覽模式、檢視模式切換 |

---

## 編輯操作

### 選取

- 左鍵點音符＝選取，拖曳＝框選
- <kbd>Ctrl</kbd>+點擊＝加選 / 取消
- <kbd>Ctrl</kbd>+<kbd>A</kbd> 全選、<kbd>Esc</kbd> 取消選取

### 放置音符

按 <kbd>2</kbd>（或工具列「放置音符」）進入放置模式，游標會變成十字。
點一下放一顆；**按住往上拖**可以直接拉出長度，tap 被拉長會自動變成長押。

放置參數在同一排：時值、左右手、寬度、類型。

### 改長押的長度

游標移到長押的**尾端**（方塊上緣）會變成上下箭頭，按住拖曳就能改長度。
**放置模式和一般模式都能用**。長度會吸附在目前選的時值的整數倍上，狀態列顯示實際毫秒數。

### 移動

| 操作 | 效果 |
|---|---|
| <kbd>↑</kbd> <kbd>↓</kbd> | 時間前後移動 1/32 音符 |
| <kbd>←</kbd> <kbd>→</kbd> | 鍵道左右移動（音高模式是改音高） |
| <kbd>Shift</kbd>+方向鍵 | 一次移動 10 格 |

右鍵選單的「時間與時值 → 整組平移」可以照拍數、自訂拍數或固定毫秒批次移動。

### 音符屬性

右鍵 → 音符屬性：

- **類型**：Tap / Soft / Long / Staccato / Slide / Trill
- **左右手**：<kbd>R</kbd> 右手、<kbd>L</kbd> 左手
- **寬度**：1～6 格
- **遊戲譜面隱藏**：音會響但畫面上不顯示（存成寄主的 `sub_note`）

> MIDI 模式下改左右手會**連 MIDI 音軌一起搬**，否則自動排譜會照音軌把手改回去。

### 音階輔助

工具列「音階輔助」打開後，在譜面上按住往上拖，就會照偵測到的調性放下一整組音。
18 種音型：音階、三度、三/七和弦琶音，以及半音階、全音階、五聲、藍調、各教會調式、減音階。

對稱音階（半音階、全音階、減音階）會以**你下筆的那個音**為起點，其餘照譜面主音。

### 手別篩選與幽靈音符

「編輯」分頁可以選只編右手或只編左手。被濾掉的那一手會變成**半透明的參考影子**——
看得到、點不到、框選和全選也不會選到。可以在偏好設定關掉影子。

---

## 播放

<kbd>Space</kbd> 播放/暫停。播放時畫面會跟著捲動，判定線固定在鍵盤上緣。

- **音源**：載入的音訊檔（wav/mp3），以及可選的第二音源
- **打擊聲**：音符過判定線時播 Tap 音效，可在「播放」分頁開關
- **沒有音樂檔也能播**：右鍵 →「播放 MIDI」用內建鋼琴音源直接聽譜面
- **播放偏移**：判定線和聲音對不上時，用「播放」分頁的偏移設定校正

播放中可以編輯，也可以拉音量——音量會即時套用，不會中斷播放。

---

## 工具選單

### 節奏與音高

| 工具 | 做什麼 |
|---|---|
| **量化** | 把音符起點對齊到拍點格線。可調**量化強度**（0～100%）、格點大小、只處理選取或整譜、要不要連長度一起 |
| **把音符吸到調內** | 把離調的音吸到最近的調內音。偵測不確定時會先問你 |
| **修補缺漏的拍點** | `beat_data` 漏拍時 BPM 會算成一半。這個會找出「間距是別人整數倍」的缺口補回去 |

### 音符整理

- **處理長條尾端**：長押尾巴和同手下一顆的最小間隔。可選「沒重疊就留間隔」（分解和弦不動）、「長押期間最多讓幾顆音進來」、或「一律裁到下一顆」
- **處理左右重疊**：同時起音的音符鍵道重疊時讓開
- **長押長度修整**：把 MIDI 的延音長度改成接近真正按鍵的長度
- **移除重複音符**：同 start 同 pitch 的重複

### 其他

- **樂曲總資訊**：BPM、總拍號、整體位移
- **以 MIDI 重建修復**：拿原始 MIDI 對齊時間、校正拍點、還原力度與踏板
- **延音踏板**：依和聲自動產生踏板，或清除
- **從別的譜面複製**：把另一份譜的力度／踏板套過來

---

## 偏好設定

**設定 → 偏好設定**，分成五個分頁：

| 分頁 | 內容 |
|---|---|
| **一般** | 語言（繁中／簡中／English） |
| **顯示** | 力度數字、強弱曲線、調性高亮、幽靈音符、狀態列、音高編號、鍵盤高度、深色模式、踏板欄、力度明暗 |
| **編輯** | 鎖調、滾輪方向、轉譜風格、匯出音訊處理、**音符寬度**、操作紀錄 |
| **效能** | 復原歷史上限（MB）、音效輸出延遲補償 |
| **快捷鍵** | 見下 |

**音符寬度**：音符要佔滿鍵道寬度的百分之幾（40~100，預設 100 = 貼滿）。
調小會在左右留出間隙，密集的譜比較看得出一顆一顆。點選判定跟著縮——所見即所點。

**復原歷史上限**：一筆歷史大約是「音符數 × 0.5KB」。譜面越大能留的步數越少（至少 8 步）。
記憶體小的機器可以調小；大譜面編輯時如果覺得吃太多記憶體，先動這裡。

---

## 快捷鍵

### 可自訂

偏好設定 → 快捷鍵。按「變更…」之後**直接按你要的鍵**，按一下就記錄起來。
<kbd>Esc</kbd> 取消、<kbd>Backspace</kbd> 清除綁定。重複綁定會標紅色。

| 動作 | 預設 |
|---|---|
| 切換檢視模式 | <kbd>1</kbd> |
| 切換放置模式 | <kbd>2</kbd> |
| 播放 / 暫停 | <kbd>Space</kbd> |
| 從頭播放全曲 | 未設定 |
| 播放目前視窗 | 未設定 |
| 停止播放 | 未設定 |

### 固定

| 鍵 | 動作 |
|---|---|
| <kbd>Ctrl</kbd>+<kbd>Z</kbd> | 復原 |
| <kbd>Ctrl</kbd>+<kbd>C</kbd> / <kbd>V</kbd> | 複製 / 貼上 |
| <kbd>C</kbd> | 就地複製 |
| <kbd>Del</kbd> | 刪除選取 |
| <kbd>T</kbd> <kbd>H</kbd> <kbd>K</kbd> | Tap / Long / Staccato |
| <kbd>R</kbd> <kbd>L</kbd> | 右手 / 左手 |
| <kbd>P</kbd> / <kbd>Shift</kbd>+<kbd>P</kbd> / <kbd>S</kbd> | 播放視窗 / 播放選取 / 停止 |
| <kbd>Tab</kbd> | 預覽模式 |
| <kbd>=</kbd> <kbd>-</kbd> | 縮放 |

---

## 匯出

| 項目 | 說明 |
|---|---|
| **另存新檔** | XML（官方格式）或 JSON |
| **匯出 MIDI** | 左右手分成兩軌，保留力度 |
| **匯出完整曲目** | 譜面＋音訊打包成遊戲的資料夾結構。會問要不要順便算一份 MIDI 鋼琴音軌 |

匯出鋼琴音軌走的是播放那條路（力度、trill/staccato 表情、CC64 延音踏板都在），
所以匯出的東西和你在編輯器裡聽到的一致。

### 「匯出完整曲目」的選項

| 選項 | 寫到哪 | 說明 |
|---|---|---|
| **分類** | 匯出根目錄的 `library.json` | 選歌畫面的頁籤。下拉會列出現有分類，也可以直接打新的；留空就是 `Other` |
| **背景影片** | `register.json` 的 `videoPath` | 路徑不含副檔名，遊戲端自己試 `.mp4` / `.webm` / `.mov`。旁邊的秒數是影片相對音訊的起點 |
| **無背景音樂** | `register.json` 的 `noBackgroundMusic` | 見下 |

**無背景音樂**是給「只有鋼琴、沒有伴奏」的曲子用的（例如練習曲）：勾了以後
匯出的不是音訊，而是一段**等長的無聲音軌**，也不會輸出鋼琴音軌，遊戲裡就只剩
玩家自己打出來的 keysound。

之所以要寫一段無聲的而不是留空，是因為遊戲端的 `audioResourcePath` 是必填
（`ExternalSongLibrary.RequireAsset` 會擋下來）。而 `noBackgroundMusic` 這個旗標
是給遊戲看的：讀到它就**強制打開合成鋼琴**，否則玩家如果停在「遊戲錄音」音源模式，
這首歌會整首沒有聲音。

> 一首歌的難度共用同一段無聲音軌時，長度要蓋過**最長**的那個譜面，
> 否則短的那段結束後歌會提早結束。

### 樂曲總資訊的「整體位移」

正值把整首往後推、負值往前提。位移**連小節線一起搬**——前面會多出一段
不屬於任何小節的空白，遊戲是讀第一個小節的 BPM 來顯示的，所以這段空白
不會影響顯示的 BPM。往前提最多只能提到 0；要求超過可移的量時，
對話框會告訴你實際移了多少。

位移只搬**譜面**，音檔長度不會跟著長：往後推 2 秒，最後 2 秒的音符就落到
音檔結束之後——遊戲讀譜面的 `music_finish_time_msec` 收歌，所以歌照跑，
只是後面沒聲音。匯出時會自動**把音訊的尾巴補靜音到蓋過譜面**（鋼琴音軌
也一樣），比譜面長的音檔則原封不動，不會裁掉尾奏。

---

## 操作紀錄

編輯器會把每次編輯的**前後差異**寫進 `qt_editor/logs/oplog-YYYYMMDD.jsonl`
（一行一個事件）。目的不是稽核，而是給演算法調校用——自動轉譜／修譜的工具
好不好，最誠實的指標是**你跑完工具之後又手動改了什麼**，改動的方向就是
演算法該調的方向。

| 事件 | 內容 |
|---|---|
| `session` | 開檔：檔名、BPM、統計輪廓 |
| `action` | 一次編輯：動作名稱、耗時、增刪改的計數、最多 30 顆的前後值、輪廓變化 |
| `undo` / `redo` | 復原。**緊接在某個 action 之後的 undo＝那個動作做錯了** |
| `mark` | 存檔之類的單點事件 |

「統計輪廓」是官方語料分析用過的那一組：音符數、密度、鍵寬分佈與平均、
同時發聲的和弦大小分佈、**同手鍵道重疊率**、左右手比例、音符種類分佈。
工具和手改都落在同一組座標上，所以可以直接比較。

掛載點是 `NoteModel.push_history()`——所有編輯的共同前置（69 個呼叫點），
動作名稱取呼叫端的函式名。工具對話框另外用 `oplog.params(...)` 把當次的旋鈕
記下來，這樣才知道是哪一組設定的結果被改回去。

紀錄是**累加**的：一天一個檔（`oplog-YYYYMMDD.jsonl`），從不刪東西，
單檔滿 20 MB 就開 `-2`、`-3`。檔案位置跟著程式走——從原始碼跑是
`qt_editor/qt_editor/logs/`，打包成 exe 是**啟動時工作目錄**底下的 `logs/`，
和 `settings.json` 同一套規則。

跑測試時**不會**寫（`_under_test`）：測試套件會大量呼叫 `push_history()`，
混進來會讓分析失真。要在測試環境強制打開就設 `NOS_OPLOG=1`。

寫檔失敗會自己關掉，不會影響編輯；每次編輯多花約 3 毫秒。
要關掉：偏好設定 →「編輯」→ 操作紀錄。紀錄是純本機檔案，已列入 `.gitignore`。

---

## 開發

### 跑測試

```bash
python -m unittest discover -s . -p "test_*.py"
```

目前 42 個測試檔、759 個測試，全部通過。測試不需要顯示器，
但 Windows 以外的環境要設 `QT_QPA_PLATFORM=offscreen`。

### 打包

```bash
python -m PyInstaller NostalgiaChartEditor.spec --noconfirm --clean
```

產出在 `dist/NostalgiaChartEditor.exe`（單檔，約 190MB，含 FluidSynth 與音源）。

> 點火測試時注意：`Start-Process` 回傳的 PID 是 PyInstaller 的**啟動器**，
> 會停在 ~9MB 且沒有視窗標題。真正的程式是同名的**子行程**（~80MB）。
> 用 `Get-Process NostalgiaChartEditor` 列出全部再看，並給它 40～60 秒解壓縮。

### 專案結構

```
qt_editor/
├── qt_editor/
│   ├── app.py                  進入點
│   ├── main_window.py          主視窗、選單、工具列、所有工具的入口
│   ├── chart_view.py           譜面畫布：繪圖、滑鼠鍵盤互動
│   ├── models.py               NoteModel：譜面資料、存讀檔、復原
│   ├── smart_chart.py          自動排譜（音高 → 鍵道）
│   ├── music_theory.py         調性偵測、音階／琶音生成
│   ├── midi_preview.py         FluidSynth 綁定、鋼琴音渲染
│   ├── midi_to_xml_converter.py
│   ├── audio_player.py         音訊播放與混音
│   └── *_dialog.py             各種對話框
├── test_*.py                   測試
├── NostalgiaChartEditor.spec   PyInstaller 設定
└── vendor/fluidsynth/          FluidSynth DLL
```

### 更詳細的說明

`使用說明.md` 有更深入的內容：各種格式的細節、自動排譜的規則與量測依據、
力度與踏板的存檔方式、疑難排解、已知限制。
