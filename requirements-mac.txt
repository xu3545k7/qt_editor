# macOS 版製譜器的相依套件（build_mac.sh 會裝這一份）
#
# 和 Windows 的差別：
#   * 沒有 simpleaudio——它沒有 Apple Silicon 的輪子，原始碼在新版 Python 上
#     編不起來。改用 sounddevice（PortAudio），介面由 audio_backend_sd.py 補成
#     和 simpleaudio 一樣，播放器不用改。
#   * 沒有 winsound（那是 Windows 內建模組）。
#   * FluidSynth 是系統的 dylib（brew install fluid-synth），不是 pip 套件。

PyQt5>=5.15.11            # 5.15.11 起才有 Apple Silicon 的輪子
mido>=1.3
sounddevice>=0.4.6
numpy>=1.24
Pillow>=10.0
soundfile>=0.12           # 匯出歌曲包時讀寫音檔
pyinstaller>=6.0
